{
  "name": "hotel-widget-server",
  "version": "1.0.0",
  "private": true,
  "description": "Serve + proxy for the Pride Hotel embeddable chat widget",
  "engines": { "node": ">=18" },
  "scripts": {
    "start": "node server.js"
  }
}
